set nocompatible

filetype on
filetype off                   " required!

set rtp+=~/.vim/bundle/vundle/
call vundle#rc()

" Vim seems to work faster with this options
set re=1 " old regex engine
set ttyfast
set lazyredraw

exe 'source ' . expand('~/.vim/settings/bundles.vim')
